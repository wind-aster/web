/** Reserved user that owns service/system messages; never a real participant. */
export const SYSTEM_USER_ID = 0;

/** Quick-reaction emoji set — kept in sync with allowedReactions on the backend. */
export const QUICK_REACTIONS = ['👍', '👎', '❤️', '😂', '😮', '😢'];
